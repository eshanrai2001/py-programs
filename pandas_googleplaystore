# -*- coding: utf-8 -*-
"""Untitled5.ipynb

///////https://www.kaggle.com/datasets/lava18/google-play-store-apps?select=googleplaystore.csv

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1Nd1MdLvoaPWtRggnyNYZVCBwxWRsDZjk
"""

import pandas as pd

data = pd.read_csv("/content/googleplaystore.csv")

# question:
# 1. Display Top 5 Rows of The Dataset
# 2. Check the Last 3 Rows of The Dataset
# 3. Find Shape of Our Dataset (Number of Rows & Number of Columns)
# 4. Get Information About Our Dataset Like Total Number Rows, Total Number of Columns, Datatypes of Each Column And Memory Requirement
# 5. Get Overall Statistics About The Dataframe
# 6. Total Number of App Titles Contain Astrology
# 7. Find Average App Rating
# 8.  Find Total Number of Unique Category
# 9. Which Category Getting The Highest Average Rating?
# 10. Find Total Number of App having 5 Star Rating
# 11. Find Average Value of Reviews
# 12. Find Total Number of Free and Paid Apps
# 13.  Which App Has Maximum Reviews?
# 14. Display Top 5 Apps Having Highest Reviews
# 15. Find Average Rating of Free and Paid Apps
# 16. Display Top  5 Apps Having Maximum Installs

data.head()

data.tail(3)

data.shape
print("rows: ",data.shape[0])

print("columns: ",data.shape[1])

data.info()

data.describe(include="all")

data.columns

len(data[data["App"].str.contains("Astrology",case=False)]) #to ignore case sensitive use case=False

data["Rating"].mean()

data["Category"].nunique()

data.groupby("Category")["Rating"].mean().sort_values(ascending=False)

len(data[data["Rating"]==5.0])

data["Reviews"].dtype  # getting datatype error and string value included 3.0M

data["Reviews"]=data["Reviews"].replace("3.0M",3.0)

data["Reviews"]= data["Reviews"].astype("float")

data["Reviews"].mean()

data["Type"].value_counts()

data[data["Reviews"].max()==data["Reviews"]]["App"]

i = data["Reviews"].sort_values(ascending=False).head(5).index

data.iloc[i]["App"]

data.groupby("Type")["Reviews"].mean()

data["Installs"]

data["Installs_1"] = data["Installs"].str.replace(",","")

data["Installs_1"] = data["Installs_1"].str.replace("+","")

data["Installs_1"].astype("int")

data["Installs_1"]= data["Installs_1"].str.replace("Free","0")

data["Installs_1"] = data["Installs_1"].astype("int")

i2 = data["Installs_1"].sort_values(ascending=False).head().index

data.iloc[i2]["App"]

